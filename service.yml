apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: nginx-namespace     
spec:
  selector:
    app: myapp
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      
